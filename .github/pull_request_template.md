## What this changes, and why

<!-- If it fixes an issue, link it. -->

## How you tested it

<!-- Anything touching the supervisor: please confirm servers still die when
     the app is kill -9'd. `cargo test --all` covers this, but say if you ran
     it, since it is the guarantee most worth protecting. -->

- [ ] `npm run build`
- [ ] `cargo test --all`
- [ ] `cargo clippy --all-targets -- -D warnings`
- [ ] `cargo fmt --all --check`

<!-- UI changes: a before/after screenshot saves a lot of back and forth. -->
